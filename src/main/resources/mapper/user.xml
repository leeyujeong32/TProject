<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="user"> <!-- mapper파일의 이름 -->
	<!-- 컬럼명과 필드명을 매핑 -->
	<insert id="insert" parameterType="user.UserVo" useGeneratedKeys="true">
		INSERT INTO member (
			id,pw,name,email,phoneno, regDate,zipcode,address
			) VALUES (
				#{id},md5(#{pw}),#{name},#{email},#{phoneno},NOW(),#{zipcode},#{address}
			)
			<selectKey keyProperty="member_id" resultType="int" order="AFTER">
			SELECT last_insert_id()
		</selectKey>	
	</insert>
	
	<select id="selectOne" parameterType="user.UserVo" resultType="user.UserVo">
		SELECT * FROM member WHERE id=#{id} and pw=md5(#{pw})
	</select>
</mapper>